name: Test

on: [workflow_call]

jobs:
  deploy:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v3
      - name: Set up node
        uses: actions/setup-node@v3
        with:
          node-version: 16.x
      - name: Run tests
        run: npm ci && npm run test
